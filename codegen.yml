overwrite: true
schema: "http://localhost:5000"
documents: null
generates:
  server/src/graphql/generated/graphql-server.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      contextType: ./src/interfaces/context#Context
      useIndexSignature: true
